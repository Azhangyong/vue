const adminToken = 'admin_token'
const userNameKey = "userName"
    // export function getToken() { //读取token
    //     return cookie.get(adminToken)
    // }
    // export function setToken(token) { //存token
    //     return cookie.set(adminToken, token)
    // }
    // export function removeToken(token) { //删除token
    //     return cookie.remove(adminToken)
    // }
    // export function setUserName(value) { //存用户名
    //     return cookie.set(userNameKey, value)
    // }
    // export function getUserName(value) { //获取用户名
    //     return cookie.get(userNameKey)
    // }
    // export function removeUserName(value) { //删除用户名
    //     return cookie.remove(userNameKey)
    // }